# 🏗️ Advanced Analytics - System Architecture

## 📊 High-Level Architecture Diagram

```
┌─────────────────────────────────────────────────────────────────────┐
│                         FRONTEND (React + TypeScript)               │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  ┌──────────────────┐  ┌──────────────────┐  ┌─────────────────┐ │
│  │ LeadsManagement  │  │ CohortAnalysis   │  │ AdvancedAnalytics│ │
│  │   Component      │  │   Dashboard      │  │   Dashboard      │ │
│  └────────┬─────────┘  └────────┬─────────┘  └────────┬────────┘ │
│           │                     │                     │           │
│           └─────────────────────┼─────────────────────┘           │
│                                 │                                 │
│                    ┌────────────▼─────────────┐                  │
│                    │   useAnalytics Hooks     │                  │
│                    │   (React Query)          │                  │
│                    └────────────┬─────────────┘                  │
│                                 │                                 │
└─────────────────────────────────┼─────────────────────────────────┘
                                  │ HTTPS
                                  │
┌─────────────────────────────────▼─────────────────────────────────┐
│                      API GATEWAY (Vercel)                         │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  ┌──────────────────┐  ┌──────────────────┐  ┌─────────────────┐ │
│  │ analytics-events │  │  lead-scoring    │  │enhanced-analytics│ │
│  │      API         │  │      API         │  │      API         │ │
│  │                  │  │                  │  │                  │ │
│  │ • Track events   │  │ • Calculate score│  │ • Revenue forecast│ │
│  │ • Query events   │  │ • Predict churn  │  │ • Pipeline velocity│ │
│  │ • Batch insert   │  │ • Get top leads  │  │ • Monthly trends │ │
│  │ • Summary stats  │  │ • At-risk leads  │  │                  │ │
│  └────────┬─────────┘  └────────┬─────────┘  └────────┬────────┘ │
│           │                     │                     │           │
└───────────┼─────────────────────┼─────────────────────┼───────────┘
            │                     │                     │
            │                     │                     │
┌───────────▼─────────────────────▼─────────────────────▼───────────┐
│                    SUPABASE (PostgreSQL)                          │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  ┌──────────────────┐  ┌──────────────────┐  ┌─────────────────┐ │
│  │analytics_events  │  │  lead_scores     │  │     leads       │ │
│  │                  │  │                  │  │                  │ │
│  │ • event_type     │  │ • lead_id        │  │ • id            │ │
│  │ • user_id        │  │ • score (0-100)  │  │ • name          │ │
│  │ • lead_id        │  │ • score_level    │  │ • status        │ │
│  │ • metadata (JSON)│  │ • churn_risk     │  │ • source        │ │
│  │ • timestamp      │  │ • breakdown      │  │ • created_at    │ │
│  │                  │  │ • updated_at     │  │ • updated_at    │ │
│  └──────────────────┘  └──────────────────┘  └─────────────────┘ │
│                                                                     │
│  Indexes:           Indexes:           Indexes:                   │
│  • event_type       • lead_id          • status                   │
│  • user_id          • score DESC       • source                   │
│  • lead_id          • score_level      • created_at               │
│  • timestamp        • churn_risk       • assigned_to              │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 🔄 Data Flow Diagrams

### **1. Lead Scoring Flow**

```
┌─────────────┐
│   User      │
│   Action    │
└──────┬──────┘
       │
       │ 1. Click "Calculate Scores"
       │
       ▼
┌─────────────────────────────────┐
│  Frontend                       │
│  useCalculateAllScores()        │
└──────┬──────────────────────────┘
       │
       │ 2. POST /api/lead-scoring/calculate-all
       │
       ▼
┌─────────────────────────────────┐
│  Backend API                    │
│  lead-scoring.js                │
└──────┬──────────────────────────┘
       │
       │ 3. Fetch all leads
       │
       ▼
┌─────────────────────────────────┐
│  Database                       │
│  SELECT * FROM leads            │
└──────┬──────────────────────────┘
       │
       │ 4. Return leads
       │
       ▼
┌─────────────────────────────────┐
│  Backend - For Each Lead        │
│  calculateLeadScore()           │
│  • Engagement: +30 pts          │
│  • Recency: +25 pts             │
│  • Source: +20 pts              │
│  • Profile: +15 pts             │
│  • Behavioral: +10 pts          │
└──────┬──────────────────────────┘
       │
       │ 5. Fetch lead activities
       │
       ▼
┌─────────────────────────────────┐
│  Database                       │
│  SELECT * FROM analytics_events │
│  WHERE lead_id = ?              │
└──────┬──────────────────────────┘
       │
       │ 6. Return activities
       │
       ▼
┌─────────────────────────────────┐
│  Backend                        │
│  predictChurn()                 │
│  • Time since contact: +40%     │
│  • Engagement decline: +30%     │
│  • Status regression: +20%      │
│  • Failed calls: +10%           │
└──────┬──────────────────────────┘
       │
       │ 7. Save results
       │
       ▼
┌─────────────────────────────────┐
│  Database                       │
│  UPSERT INTO lead_scores        │
│  (lead_id, score, churn_risk)   │
└──────┬──────────────────────────┘
       │
       │ 8. Return success
       │
       ▼
┌─────────────────────────────────┐
│  Frontend                       │
│  Show "1000 leads scored!"      │
│  Refresh dashboards             │
└─────────────────────────────────┘
```

---

### **2. Activity Tracking Flow**

```
┌─────────────┐
│   User      │
│   Views     │
│   Lead      │
└──────┬──────┘
       │
       │ 1. Lead details opened
       │
       ▼
┌─────────────────────────────────┐
│  Frontend Component             │
│  LeadsManagement.tsx            │
│  handleLeadClick()              │
└──────┬──────────────────────────┘
       │
       │ 2. Track event
       │
       ▼
┌─────────────────────────────────┐
│  Frontend Hook                  │
│  useTrackEvent()                │
│  trackEvent.mutate({            │
│    type: 'lead_viewed',         │
│    leadId: 'abc123',            │
│    metadata: { duration: 45 }   │
│  })                             │
└──────┬──────────────────────────┘
       │
       │ 3. POST /api/analytics-events
       │    Body: { type, leadId, metadata }
       │
       ▼
┌─────────────────────────────────┐
│  Backend API                    │
│  analytics-events.js            │
│  trackEvent()                   │
└──────┬──────────────────────────┘
       │
       │ 4. Enrich with metadata
       │    • ip_address
       │    • user_agent
       │    • timestamp
       │    • session_id
       │
       ▼
┌─────────────────────────────────┐
│  Database                       │
│  INSERT INTO analytics_events   │
│  (event_type, lead_id, ...)     │
└──────┬──────────────────────────┘
       │
       │ 5. Return created event
       │
       ▼
┌─────────────────────────────────┐
│  Frontend                       │
│  Event tracked successfully     │
│  (Silent - no UI update)        │
└─────────────────────────────────┘
```

---

### **3. Revenue Forecasting Flow**

```
┌─────────────┐
│   User      │
│   Opens     │
│  Dashboard  │
└──────┬──────┘
       │
       │ 1. Navigate to /advanced-analytics
       │
       ▼
┌─────────────────────────────────┐
│  Frontend Component             │
│  AdvancedAnalyticsDashboard     │
└──────┬──────────────────────────┘
       │
       │ 2. useRevenueForecast()
       │
       ▼
┌─────────────────────────────────┐
│  Frontend Hook                  │
│  GET /enhanced-analytics/       │
│      revenue-forecast           │
└──────┬──────────────────────────┘
       │
       │ 3. API Request
       │
       ▼
┌─────────────────────────────────┐
│  Backend API                    │
│  enhanced-analytics.js          │
│  handleRevenueForecast()        │
└──────┬──────────────────────────┘
       │
       │ 4. Fetch all leads
       │
       ▼
┌─────────────────────────────────┐
│  Database                       │
│  SELECT * FROM leads            │
└──────┬──────────────────────────┘
       │
       │ 5. Calculate metrics
       │
       ▼
┌─────────────────────────────────┐
│  Backend - Analysis             │
│                                 │
│  For each lead:                 │
│  • Get source conversion rate   │
│  • Apply status multiplier      │
│  • Calculate expected value     │
│                                 │
│  Hot lead from Referral:        │
│  Expected = $50K × 0.8 × 0.7    │
│           = $28,000             │
│                                 │
│  Sum all expected values        │
│  Calculate confidence interval  │
└──────┬──────────────────────────┘
       │
       │ 6. Return forecast
       │    {
       │      currentRevenue: 5M,
       │      forecast: {
       │        expected: 2.5M,
       │        optimistic: 3.2M,
       │        pessimistic: 1.8M
       │      }
       │    }
       │
       ▼
┌─────────────────────────────────┐
│  Frontend                       │
│  Display forecast cards         │
│  Show pipeline breakdown        │
│  Render monthly trends          │
└─────────────────────────────────┘
```

---

## 🗃️ Database Schema Relationships

```
┌─────────────────────────────────────────────────────────────────┐
│                        Database Schema                          │
└─────────────────────────────────────────────────────────────────┘

    users                    leads                 lead_scores
┌──────────┐          ┌──────────────┐        ┌──────────────┐
│ id (PK)  │◄─────────│ assigned_to  │        │ id (PK)      │
│ name     │          │ id (PK)      │◄───────│ lead_id (FK) │
│ email    │          │ name         │        │ score        │
│ role     │          │ status       │        │ score_level  │
└────┬─────┘          │ source       │        │ churn_risk   │
     │                │ created_at   │        │ churn_level  │
     │                │ updated_at   │        └──────────────┘
     │                └──────┬───────┘
     │                       │
     │                       │
     │                       │
     │                       │
     │        analytics_events
     │        ┌──────────────────────┐
     │        │ id (PK)              │
     └────────┤ user_id (FK)         │
              │ lead_id (FK)         │
              │ event_type           │
              │ metadata (JSONB)     │
              │ timestamp            │
              │ duration_seconds     │
              │ session_id           │
              │ ip_address           │
              └──────────────────────┘

Relationships:
• users.id → analytics_events.user_id (1:N)
• leads.id → analytics_events.lead_id (1:N)
• leads.id → lead_scores.lead_id (1:1)
• users.id → leads.assigned_to (1:N)
```

---

## 🔐 Authentication Flow

```
┌─────────────┐
│   User      │
│   Logs In   │
└──────┬──────┘
       │
       │ 1. POST /api/auth/login
       │    { email, password }
       │
       ▼
┌─────────────────────────────────┐
│  Backend Auth                   │
│  Verify credentials             │
│  Generate JWT token             │
└──────┬──────────────────────────┘
       │
       │ 2. Return token
       │    { token: "eyJhbG..." }
       │
       ▼
┌─────────────────────────────────┐
│  Frontend                       │
│  Save to localStorage           │
│  Set in axios headers           │
└──────┬──────────────────────────┘
       │
       │ 3. All subsequent requests
       │    Authorization: Bearer eyJhbG...
       │
       ▼
┌─────────────────────────────────┐
│  Backend API                    │
│  verifyToken(req)               │
│  Decode JWT                     │
│  Extract userId                 │
└──────┬──────────────────────────┘
       │
       │ 4. Process request
       │    with authenticated userId
       │
       ▼
┌─────────────────────────────────┐
│  Database                       │
│  Query with user permissions    │
└─────────────────────────────────┘
```

---

## 🚀 Deployment Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                        Production Setup                         │
└─────────────────────────────────────────────────────────────────┘

                    ┌──────────────┐
                    │   User's     │
                    │   Browser    │
                    └──────┬───────┘
                           │
                           │ HTTPS
                           │
        ┌──────────────────┴──────────────────┐
        │                                     │
        ▼                                     ▼
┌────────────────┐                  ┌────────────────┐
│ Frontend       │                  │ Backend        │
│ (Vercel)       │                  │ (Vercel)       │
│                │                  │                │
│ • React App    │                  │ • Node.js      │
│ • Static Files │                  │ • Serverless   │
│ • CDN          │                  │ • API Routes   │
└────────┬───────┘                  └────────┬───────┘
         │                                   │
         │                                   │
         │           ┌───────────────────────┘
         │           │
         │           │ PostgreSQL Protocol
         │           │
         └───────────▼───────────────┐
                 ┌───────────────────┴──┐
                 │   Supabase           │
                 │   (PostgreSQL)       │
                 │                      │
                 │ • Database           │
                 │ • Auth               │
                 │ • Storage            │
                 │ • Real-time          │
                 └──────────────────────┘

Hosting:
• Frontend: Vercel Edge Network (CDN)
• Backend: Vercel Serverless Functions (AWS Lambda)
• Database: Supabase (AWS RDS + Extensions)

Performance:
• Frontend: < 1s page load (CDN)
• Backend: < 200ms API response
• Database: < 50ms query time (indexed)
```

---

## 📊 Performance Optimization

### **Database Indexing Strategy**

```sql
-- analytics_events (for fast queries)
CREATE INDEX idx_events_type ON analytics_events(event_type);
CREATE INDEX idx_events_timestamp ON analytics_events(timestamp DESC);
CREATE INDEX idx_events_lead ON analytics_events(lead_id);
CREATE INDEX idx_events_user_type ON analytics_events(user_id, event_type);

-- lead_scores (for sorting and filtering)
CREATE INDEX idx_lead_scores_score ON lead_scores(score DESC);
CREATE INDEX idx_lead_scores_churn ON lead_scores(churn_risk DESC);
CREATE INDEX idx_lead_scores_lead ON lead_scores(lead_id);

-- leads (existing + enhancements)
CREATE INDEX idx_leads_status ON leads(status);
CREATE INDEX idx_leads_source ON leads(source);
CREATE INDEX idx_leads_created ON leads(created_at DESC);
```

### **React Query Caching**

```typescript
// Stale time settings
useLeadScore: 5 minutes       // Scores change slowly
useRevenueForecast: 10 minutes // Recalc expensive
useAnalyticsEvents: 2 minutes  // More dynamic
useRecentActivities: 1 minute  // Real-time feel
```

---

## 🔄 Background Jobs (Future)

```
┌─────────────────────────────────────────────────────────────────┐
│                    Automated Tasks (Cron)                       │
└─────────────────────────────────────────────────────────────────┘

Daily (2 AM):
┌─────────────────────────────────┐
│ Recalculate Lead Scores         │
│ • For all active leads          │
│ • Update churn predictions      │
│ • Send high churn risk alerts   │
└─────────────────────────────────┘

Weekly (Sunday 1 AM):
┌─────────────────────────────────┐
│ Generate Performance Reports    │
│ • Weekly conversion rates       │
│ • Pipeline velocity trends      │
│ • Top performing sources        │
└─────────────────────────────────┘

Monthly (1st of month):
┌─────────────────────────────────┐
│ Cohort Analysis Refresh         │
│ • Calculate new cohort          │
│ • Update historical data        │
│ • Email reports to managers     │
└─────────────────────────────────┘
```

---

## 🎨 Component Hierarchy

```
App.tsx
│
├── Router
│   │
│   ├── /advanced-analytics
│   │   └── AdvancedAnalyticsDashboard
│   │       ├── Revenue Forecast Section
│   │       │   ├── Forecast Cards (4)
│   │       │   ├── Pipeline by Status Grid
│   │       │   └── Monthly Trends Table
│   │       │
│   │       ├── Pipeline Velocity Section
│   │       │   ├── Velocity Cards (4)
│   │       │   ├── Conversion Metrics
│   │       │   └── Health Recommendation
│   │       │
│   │       └── Lead Lists Section
│   │           ├── Top Leads (useTopLeads)
│   │           │   └── LeadScoreBadge × 10
│   │           └── At-Risk Leads (useAtRiskLeads)
│   │               └── Churn indicators × 10
│   │
│   ├── /cohort-analysis
│   │   └── CohortAnalysis
│   │       ├── Summary Cards (4)
│   │       ├── Cohort Table
│   │       │   └── Rows (12 months)
│   │       └── Insights Cards (2)
│   │
│   └── /leads
│       └── LeadsManagement
│           ├── Lead Cards
│           │   └── LeadScoreBadge (new)
│           └── Lead Details Modal
│               └── LeadScoreCard (new)
│
└── Hooks
    └── useAnalytics.ts
        ├── useLeadScore
        ├── useCalculateLeadScore
        ├── useTopLeads
        ├── useAtRiskLeads
        ├── useTrackEvent
        ├── useRevenueForecast
        └── usePipelineVelocity
```

---

This architecture supports:
✅ Scalability to 100K+ leads
✅ Sub-second API responses
✅ Real-time activity tracking
✅ Predictive analytics
✅ Beautiful, responsive UI
✅ Production-ready deployment
